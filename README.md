# Modal Memory

Browser extension starter for Chrome and Microsoft Edge that temporarily saves modal form data by page URL and modal name, then highlights restorable fields on demand from the extension popup.

## Setup

```sh
npm install
npm run build
npm test
```

For development builds:

```sh
npm run dev
```

Load the generated `dist` directory as an unpacked extension in Chrome or Edge.
