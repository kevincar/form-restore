{
  "name": "modmed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -- --watch",
    "build": "tsc && node scripts/copy-static.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
